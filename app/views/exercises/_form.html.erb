<%= form_for(exercise) do |f| %>
  <% if exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exercise.errors.count, "error") %> prohibited this exercise from being saved:</h2>

      <ul>
      <% exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :weight %>
    <%= f.text_field :weight %>
  </div>

  <div class="field">
    <%= f.label :sets %>
    <%= f.number_field :sets %>
  </div>

  <div class="field">
    <%= f.label :reps %>
    <%= f.number_field :reps %>
  </div>

  <div class="field">
    <%= f.label :routine_id %>
    <%= f.text_field :routine_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
